import * as e from "littlejsengine";
import sounds from "sound:../cat_sounds";

export const leap = new e.Sound(sounds[0]);
export const meow = new e.Sound(sounds[1]);
export const clover = new e.Sound(sounds[2]);
